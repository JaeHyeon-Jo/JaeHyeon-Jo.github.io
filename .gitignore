# 개인 기록의 md는 public 저장소에 올리지 않는다.
# 한줄평·가격·GPS가 그대로 들어 있다.
# 파일명 표시는 server.py의 PERSONAL_SUFFIX와 맞춰져 있다.
contents/*_개인.md

# 사진(image/)은 제외하지 않는다 — 개인 기록도 블로거에는 발행되고,
# 블로거 본문이 이 사진 URL을 직접 참조하므로 빼면 블로그 사진이 깨진다.

.DS_Store
저장소 루트 이전 작업.md
